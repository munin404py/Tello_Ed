# slam-ad プロジェクト説明書

## 1. 概要

`slam-ad`リポジトリは、SLAM (Simultaneous Localization and Mapping) 技術を基盤とした自動運転 (Autonomous Driving) のためのソフトウェアプロジェクトです。  

このプロジェクトは、センサーデータ（カメラ映像など）をリアルタイムで処理し、車両の自己位置を推定すると同時に、周囲の環境地図を生成する機能を提供します。

## 2. アーキテクチャ

本プロジェクトは、主に以下のコンポーネントで構成されています。

-   **`main.py`**: アプリケーション全体を起動し、各コンポーネントを連携させるメインスクリプトです。
-   **`backend` ディレクトリ**: Pythonで実装されたサーバーサイドのロジックが含まれます。SLAMアルゴリズムの実行、データ処理、APIの提供などを担当します。
-   **`frontend` ディレクトリ**: JavaScript, HTML/CSSで実装されたユーザーインターフェースです。リアルタイムで生成される地図や車両の状態を可視化します。
-   **`setup.sh`**: プロジェクトの実行に必要な依存関係（ライブラリなど）を自動でインストールするためのシェルスクリプトです。
-   **各種ドキュメント (`.md`ファイル)**:
    -   `README.md`: プロジェクトの基本的な情報が記載されています。
    -   `ARCHITECTURE.md`: システム全体の構成や設計に関する詳細な説明です。
    -   `QUICK_START.md`: プロジェクトを素早く動かすための手順書です。
    -   `SETUP_GUIDE.md`: 詳細なセットアップ手順が記載されています。
    -   `SLAM_INTEGRATION.md`: 外部のSLAMシステムと連携するためのガイドです。

## 3. セットアップと実行方法

### 必要なもの

-   Python 3.x
-   Git

### 手順

1.  **リポジトリのクローン**:
    ```bash
    git clone https://github.com/munin404py/slam-ad.git
    cd slam-ad
    ```

2.  **セットアップスクリプトの実行**:
    以下のコマンドを実行し、必要なライブラリやツールをインストールします。
    ```bash
    bash setup.sh
    ```
    これにより、`requirements.txt`に記載されたPythonライブラリなどがセットアップされます。

3.  **アプリケーションの実行**:
    以下のコマンドでメインプログラムを起動します。
    ```bash
    python main.py
    ```

4.  **フロントエンドへのアクセス**:
    Webブラウザを開き、指定されたアドレス（例: `http://localhost:8000`）にアクセスすると、システムのUIが表示されます。

## 4. 主な機能

-   **リアルタイムSLAM**: カメラなどのセンサーからの入力に基づき、リアルタイムで自己位置推定と地図作成を行います。
-   **Webベースの可視化**: 生成された地図、車両の軌跡、現在のセンサー情報などをWebブラウザ上で確認できます。
-   **モジュール化された設計**: `backend`と`frontend`が分離しており、機能の拡張や変更が容易です。